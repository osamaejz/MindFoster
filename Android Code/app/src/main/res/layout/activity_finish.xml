<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".finishActivity"
    android:background="@drawable/blue_purple_back">

    <TextView
        android:id="@+id/output_text"
        android:layout_width="331dp"
        android:layout_height="138dp"
        android:text="Great..!!\n You have successfully completed the session."
        android:textAlignment="center"
        android:textSize="24sp"
        android:textColor="@color/colorText"
        app:layout_constraintBottom_toTopOf="@+id/another_session"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:gravity="center_horizontal" />

    <Button
        android:id="@+id/another_session"
        android:layout_width="138dp"
        android:layout_height="46dp"
        android:layout_marginBottom="36dp"
        android:background="#71B46CFF"
        android:text="Another session?"
        android:textColor="@color/colorText"
        app:layout_constraintBottom_toTopOf="@+id/exit"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/exit"
        android:layout_width="138dp"
        android:layout_height="49dp"
        android:layout_marginBottom="140dp"
        android:background="#71B46CFF"
        android:text="Finish and Exit"
        android:textColor="@color/colorText"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>